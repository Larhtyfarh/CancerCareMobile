import React from "react";
import { View,Text } from "react-native";
import RF from "../../utils/RF";

function Appointment({}) {
    return (
        <View style={{width: RF(280),height: RF(200),backgroundColor: "#FFF",position: "absolute",top: 80,zIndex: 10,borderRadius: RF(20), elevation: 2,shadowColor: "rgba(0,0,0,0.5)",shadowOffset: {width: 0,height: 4},shadowOpacity: 0.30,shadowRadius: 4.65,zIndex: 20}}>

        </View>
    )
} 


export default Appointment;